package com.dtreb.security;

/**
 * Predefined authorities used for the OAuth2 workflow.
 *
 * @author dtreb
 */
public enum Authorities {
    // Regular registered and logged-in user
    ROLE_USER,
    // Special admin user (currently not used)
    ROLE_ADMIN
}
